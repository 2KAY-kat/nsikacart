// blank canvas the editorial page and the algorithimc paage system whatever....

let editProductHTML = '';

editProduct.forEach((editProduct) => {
    editProductHTML +=
    `
    
    `;

});

document.querySelector('.container').innnerHTML = editProductHTML;

function editButton(editProduct) {
    if (editButton !=== upladButton) {
        
    }
}